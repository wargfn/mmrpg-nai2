[ollama]
# Host for Open WebUI; /ollama is the Ollama proxy path
host = "http://127.0.0.1:3000/ollama"

# Default model used by the CLI if --model is not passed
model = "llama3.3"

# Optional API key used as Authorization header when set
api_key = ""
